# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-functions-mcp-demo
metadata:
    template: azd-init@1.24.3
services:
    mcp-demo:
        project: .
        host: function
        language: dotnet
